package lubie.dev.splitwise.mapper;

import lubie.dev.splitwise.dto.UserRequestDTO;
import lubie.dev.splitwise.dto.UserResponseDTO;
import lubie.dev.splitwise.model.Users;
import org.springframework.beans.factory.annotation.Autowired;

public class UserMapper {

//    public  static Users toEntity(UserRequestDTO dto){
//          Users user = new Users();
//          user.setUsername(dto.getUsername());
//          user.setPassword(dto.getPassword());
//          user.setEmail(dto.getEmail());
//
//          return  user;
//    }

//    public  static UserResponseDTO toDTO(Users user){
//        return new UserResponseDTO(user.getId() , user.getUsername() , user.getEmail());
//    }
}
